name: un:lock Continuous Deployment

on:
  push:
    branches: [ "main", "dev" ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build with Gradle
        run: ./gradlew build -x test --no-daemon

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Extract Metadata
        id: meta
        run: |
          if [ "${{ github.ref_name }}" = "main" ]; then
            echo "TAG=prod" >> $GITHUB_OUTPUT
            echo "SCALE=3" >> $GITHUB_OUTPUT
          else
            echo "TAG=dev" >> $GITHUB_OUTPUT
            echo "SCALE=1" >> $GITHUB_OUTPUT
          fi

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          platforms: linux/arm64
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/unlock-backend:${{ steps.meta.outputs.TAG }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: Execute Remote Deployment (SSH)
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            export PATH=$PATH:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin
            PROJECT_PATH="/Users/changbro/Desktop/unlock-backend"
            
            if [ -d "$PROJECT_PATH/.git" ]; then
              cd $PROJECT_PATH
              git fetch origin
              git reset --hard origin/${{ github.ref_name }}
            else
              mkdir -p $PROJECT_PATH
              cd $PROJECT_PATH
              git clone -b ${{ github.ref_name }} https://github.com/${{ github.repository }}.git .
            fi
            
            echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin
            
            if [ "${{ github.ref_name }}" = "main" ]; then
              TAG="prod"
              SCALE=3
              ENV_FILE=".env.prod"
              cat << 'EOF' > .env.prod
            ${{ secrets.ENV_PROD }}
            EOF
            else
              TAG="dev"
              SCALE=1
              ENV_FILE=".env.dev"
              cat << 'EOF' > .env.dev
            ${{ secrets.ENV_DEV }}
            EOF
            fi
            
            # [핵심 수정] Docker Hub에서 최신 이미지를 가져옵니다.
            docker pull ${{ secrets.DOCKER_USERNAME }}/unlock-backend:$TAG
            
            # [핵심 수정] --build 옵션을 제거하여 맥북에서 이미지를 다시 굽지 않도록 합니다.
            # 이미 Docker Hub에서 최신 이미지를 pull 받았으므로, 그대로 실행만 하면 됩니다.
            docker-compose --env-file $ENV_FILE -p unlock-$TAG up -d --scale app=$SCALE app
            
            # Nginx 갱신
            docker exec unlock-nginx-$TAG nginx -s reload || true
            docker image prune -f
            docker logout
